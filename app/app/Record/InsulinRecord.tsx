import { ModalView } from "@/component";
import React from "react";
import { Text } from "react-native";

const AddInsulinRecord = () => {
  return (
    <ModalView h={50}>
      <Text>Add Insulin Record</Text>
    </ModalView>
  );
};

export default AddInsulinRecord;
